# Core dependencies
# torch 由基础镜像提供（2.3.1）；勿在此再装新版以免冲突
numpy>=1.21.0
pandas>=1.3.0
tqdm>=4.62.0

# ESM2 编码器：transformers>=4.52 要求 torch>=2.4，与镜像 torch 2.3.1 不兼容
transformers==4.46.3
accelerate==0.34.2

# For training metrics
scipy>=1.7.0

# For logging (optional)
# tensorboard>=2.10.0
# wandb>=0.13.0
